<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Browser Versions</title>
  <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
  <header>
    <h1>Current Browser Versions</h1>
    <p>Last updated: {{ buildTime }}</p>
  </header>

  <main>
    <div class="browser-grid">
      {% for browser in browsers %}
     <article class="browser-card">
        <div class="browser-header">
    <img src="/assets/{{ browser.key }}.svg" alt="{{ browser.name }} logo" class="browser-logo" />
    <div class="browser-title">
      <h2 class="browser-name">{{ browser.name }}</h2>
      {% if browser.current.release_date %}
      <span class="last-updated">Updated {{ browser.current.release_date | relativeDate }}</span>
      {% endif %}
    </div>
  </div>
  
  <div class="version-info">
    <span class="version-label">Current Version</span>
    <span class="version-number">{{ browser.current.version }}</span>
    <span> Built on {{ browser.current.engine }} </span>
  </div>
  
  {% if browser.current.release_date %}
  <div class="release-date">
    <time datetime="{{ browser.current.release_date }}">Released on
      <span>{{ browser.current.release_date | readableDate }}<span>
    </time>
  </div>
  {% endif %}

   {% if browser.next %}
  <div class="release-date">
    <time datetime="{{ browser.next.release_date }}"> The next version of {{ browser.name }} will be {{ browser.next.version }}, set to be released on
      <span>{{ browser.next.release_date | readableDate }}<span>
    </time>
  </div>
  {% endif %}
  
  {% if browser.current.release_notes %}
  <a href="{{ browser.current.release_notes }}" target="_blank" rel="noopener noreferrer" class="release-notes-link">
    View Release Notes
    <svg class="external-icon" width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M10.5 7.5V11.5H2.5V3.5H6.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
      <path d="M8 2.5H11.5V6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
      <path d="M11.5 2.5L7 7" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
    </svg>
  </a>
  {% endif %}
</article>
      {% endfor %}
    </div>
  </main>
  <section class="api-section">
    <h2>API</h2>
    <p>
        This site exposes a static JSON API with the latest stable versions
        of major browsers.
    </p>

    <div class="api-endpoint">
        <code>
            <span class="method">GET</span>
            <a class="url" href="https://browserversions.netlify.app/api/"><span>https://browserversions.netlify.app/api/</span></a>
        </code>
    </div>
</section>

</body>
</html>
